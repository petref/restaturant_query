type Restaurant implements Timestamps {
    id: ID!
    name: String,
    address: String,
    email: String,
    
     # Interface required
    createdAt: Date!
    updatedAt: Date!
}

# Inputs
input RestaurantCreateInput {
  name: String!
}

input RestaurantUpdateInput {
  name: String
}